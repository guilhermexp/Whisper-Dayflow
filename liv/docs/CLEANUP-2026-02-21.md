# Cleanup Report - 2026-02-22

## Scope
- Padronização de package manager
- Unificação de backend IPC
- Remoção de duplicidades/artefatos
- Atualização de documentação técnica

## Applied changes
- Removed lockfiles not used by project: `bun.lock`, `package-lock.json`.
- Removed tracked build artifacts: `tsconfig.node.tsbuildinfo`, `tsconfig.web.tsbuildinfo`.
- Removed temporary screenshots from repo root/scripts.
- Consolidated AI docs folder from `ai_docs/` into `docs/ai_docs/`.
- Standardized scripts to `pnpm` in `package.json`.
- Removed orphan component: `src/renderer/src/components/setup.tsx`.

## IPC unification
- Added missing compatibility procedures into `src/main/tipc.ts` (settings, key store, index/tags/highlights/links/file ops/piles config).
- Migrated renderer calls from raw legacy channels to `tipcClient` in contexts/hooks/pages.
- Removed legacy backend loader and handlers:
  - `src/main/pile-ipc.ts`
  - `src/main/pile-handlers/*`
- `src/main/index.ts` no longer imports legacy IPC bootstrap.

## Validation
- `pnpm run typecheck` passed after migration.

## Remaining known items
- `src/main/pile-utils/*` stays as domain logic modules (now called by tipc procedures).
- `nanobot-ref/` and `docs/NANOBOT-INTEGRATION.md` intentionally untouched (new implementation provided by user).
